---
phase: 02.1-domain-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/ai/mod.rs
  - server/src/ai/db.rs
  - server/src/ai/models.rs
  - server/src/ai/routes.rs
  - server/src/cloud_accounts/mod.rs
  - server/src/cloud_accounts/reader.rs
  - server/src/cloud_accounts/resolver.rs
  - server/src/routes/mod.rs
  - server/src/main.rs
  - server/src/jobs/runner.rs
  - server/src/graph/routes.rs
  - server/src/validation/routes.rs
  - server/src/script/rewrite.rs
autonomous: true
requirements:
  - API-10

must_haves:
  truths:
    - "conversations/ directory no longer exists — all conversation code lives in ai/"
    - "cloud/ directory no longer exists — all cloud infrastructure code lives in cloud_accounts/"
    - "All use crate::conversations:: paths replaced with use crate::ai::"
    - "All use crate::cloud:: paths replaced with use crate::cloud_accounts::"
    - "cargo build compiles with zero errors after both merges"
  artifacts:
    - path: "server/src/ai/db.rs"
      provides: "Conversation database queries (moved from conversations/db.rs)"
    - path: "server/src/ai/models.rs"
      provides: "Conversation data types (moved from conversations/models.rs)"
    - path: "server/src/ai/routes.rs"
      provides: "Conversation route handlers (moved from conversations/routes.rs)"
    - path: "server/src/cloud_accounts/reader.rs"
      provides: "Cloud data reader (moved from cloud/reader.rs)"
    - path: "server/src/cloud_accounts/resolver.rs"
      provides: "Cloud output resolver (moved from cloud/resolver.rs)"
  key_links:
    - from: "server/src/ai/routes.rs"
      to: "ai/client, ai/models, ai/db"
      via: "use super:: sibling imports"
      pattern: "use super::"
    - from: "server/src/routes/mod.rs"
      to: "crate::ai::routes"
      via: "handler references in router"
      pattern: "crate::ai::routes::"
    - from: "server/src/cloud_accounts/mod.rs"
      to: "cloud_accounts/reader, cloud_accounts/resolver"
      via: "pub mod declarations + re-exports of build_store, is_cloud_url, is_data_path"
      pattern: "pub mod reader.*pub mod resolver"
---

<objective>
Absorb `conversations/` into `ai/` and `cloud/` into `cloud_accounts/` — the two smallest domain merges.

Purpose: These two modules are scattered fragments that belong inside established domain folders. Merging them first reduces the number of top-level modules and simplifies later merges in Plan 02 (discovery, jobs) by establishing clean import paths early.

Output: `ai/` contains all conversation code alongside the LLM client; `cloud_accounts/` contains all cloud infrastructure alongside account CRUD. The `conversations/` and `cloud/` directories are deleted. `main.rs` loses 2 `mod` declarations. `cargo build` passes.
</objective>

<execution_context>
@/Users/angel.ip/.claude/get-shit-done/workflows/execute-plan.md
@/Users/angel.ip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1-domain-consolidation-merge-scattered-modules-into-cohesive-domain-folders/02.1-RESEARCH.md

Source files to move:
@server/src/conversations/mod.rs
@server/src/conversations/db.rs
@server/src/conversations/models.rs
@server/src/conversations/routes.rs
@server/src/ai/mod.rs
@server/src/ai/client.rs
@server/src/cloud/mod.rs
@server/src/cloud/reader.rs
@server/src/cloud/resolver.rs
@server/src/cloud_accounts/mod.rs

Key callers that need import updates:
@server/src/routes/mod.rs
@server/src/main.rs
@server/src/jobs/runner.rs
@server/src/graph/routes.rs
@server/src/validation/routes.rs
@server/src/script/rewrite.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Absorb conversations/ into ai/</name>
  <files>
    server/src/ai/mod.rs
    server/src/ai/db.rs
    server/src/ai/models.rs
    server/src/ai/routes.rs
    server/src/conversations/ (deleted)
    server/src/routes/mod.rs
    server/src/main.rs
  </files>
  <action>
Move conversation files into `ai/`:

1. **Copy files** from `conversations/` to `ai/`:
   - `conversations/db.rs` -> `ai/db.rs`
   - `conversations/models.rs` -> `ai/models.rs`
   - `conversations/routes.rs` -> `ai/routes.rs`

2. **Update `ai/routes.rs` imports** (the moved file):
   - Change `use crate::ai::client::{AiError, ask_llm};` to `use super::client::{AiError, ask_llm};` (now a sibling)
   - Change `use crate::conversations::models::{Conversation, ConversationMessage};` to `use super::models::{Conversation, ConversationMessage};` (now a sibling)
   - Change `use crate::conversations::db;` to `use super::db;` (now a sibling)
   - Keep all other `use crate::` imports unchanged (they reference external modules like `crate::db`, `crate::pipeline`, etc.)

3. **Update `ai/mod.rs`** to declare the new submodules:
   - Add `pub mod db;`
   - Add `pub mod models;`
   - Add `pub mod routes;`
   - Keep existing `pub mod client;`

4. **Update `routes/mod.rs`** handler references:
   - Replace all `crate::conversations::routes::` with `crate::ai::routes::`
   - Handlers affected: `ask_discover`, `list_conversations`, `create_conversation`, `get_conversation_messages`, `rename_conversation`, `delete_conversation`

5. **Update `main.rs`**:
   - Remove `mod conversations;`
   - Keep `mod ai;` (already exists)

6. **Delete** the entire `conversations/` directory.

7. **Run `cargo build`** to verify compilation. Fix any remaining `crate::conversations::` references found by the compiler.

Do NOT change any behavior, error handling, or API paths. This is purely a file move + import update.
  </action>
  <verify>
    <automated>cd /Users/angel.ip/dev/kanzo/keasy/keasy/server && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `conversations/` directory deleted
    - `ai/` contains `mod.rs`, `client.rs`, `db.rs`, `models.rs`, `routes.rs`
    - No `crate::conversations::` references remain anywhere in the codebase
    - `cargo build` compiles with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Absorb cloud/ into cloud_accounts/</name>
  <files>
    server/src/cloud_accounts/mod.rs
    server/src/cloud_accounts/reader.rs
    server/src/cloud_accounts/resolver.rs
    server/src/cloud/ (deleted)
    server/src/jobs/runner.rs
    server/src/graph/routes.rs
    server/src/validation/routes.rs
    server/src/script/rewrite.rs
    server/src/main.rs
  </files>
  <action>
Move cloud infrastructure files into `cloud_accounts/`:

1. **Copy files** from `cloud/` to `cloud_accounts/`:
   - `cloud/reader.rs` -> `cloud_accounts/reader.rs`
   - `cloud/resolver.rs` -> `cloud_accounts/resolver.rs`

2. **Move `cloud/mod.rs` logic into `cloud_accounts/mod.rs`**:
   - Copy the `build_store()`, `is_cloud_url()`, `is_data_path()` functions and any associated imports/macros from `cloud/mod.rs` into `cloud_accounts/mod.rs` (inline them — they are small utility functions)
   - Add `pub mod reader;` and `pub mod resolver;` declarations
   - Ensure all functions that were `pub` in `cloud/mod.rs` remain `pub` in `cloud_accounts/mod.rs`

3. **Update `cloud_accounts/reader.rs`** imports:
   - Any `use super::` references that pointed to `cloud/mod.rs` functions now point to `cloud_accounts/mod.rs` — `super::` still resolves correctly since the parent changed from `cloud` to `cloud_accounts`, and the functions are now in `cloud_accounts/mod.rs`. Verify each `use super::` line.

4. **Update `cloud_accounts/resolver.rs`** imports:
   - Same as reader — verify all `use super::` references resolve to functions now in `cloud_accounts/mod.rs`

5. **Update all external callers** (search for `crate::cloud::` everywhere):
   - `jobs/runner.rs`: `use crate::cloud::resolver::CloudOutputResolver` -> `use crate::cloud_accounts::resolver::CloudOutputResolver`
   - `graph/routes.rs`: `use crate::cloud::reader` -> `use crate::cloud_accounts::reader`
   - `validation/routes.rs`: `use crate::cloud::reader` -> `use crate::cloud_accounts::reader`
   - `script/rewrite.rs`: `use crate::cloud::is_data_path` -> `use crate::cloud_accounts::is_data_path`
   - Any other files found by `grep -rn "crate::cloud::" server/src/`

6. **Update `main.rs`**:
   - Remove `mod cloud;`
   - Keep `mod cloud_accounts;`

7. **Delete** the entire `cloud/` directory.

8. **Run `cargo build`** to verify compilation. Fix any remaining `crate::cloud::` references.

Do NOT change any behavior. The `build_store`, `is_cloud_url`, `is_data_path` functions must remain functionally identical.
  </action>
  <verify>
    <automated>cd /Users/angel.ip/dev/kanzo/keasy/keasy/server && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `cloud/` directory deleted
    - `cloud_accounts/` contains `mod.rs`, `db.rs`, `errors.rs`, `models.rs`, `routes.rs`, `reader.rs`, `resolver.rs`
    - `cloud_accounts/mod.rs` exports `build_store()`, `is_cloud_url()`, `is_data_path()`, `reader`, `resolver`
    - No `crate::cloud::` references remain anywhere in the codebase (only `crate::cloud_accounts::`)
    - `cargo build` compiles with zero errors
  </done>
</task>

</tasks>

<verification>
1. `cargo build` — zero errors, zero warnings
2. `grep -rn "crate::conversations::" server/src/` — zero matches
3. `grep -rn "crate::cloud::" server/src/` — zero matches (only `crate::cloud_accounts::` allowed)
4. `ls server/src/conversations/` — directory does not exist
5. `ls server/src/cloud/` — directory does not exist
6. `grep -c "mod conversations" server/src/main.rs` — 0
7. `grep -c "mod cloud;" server/src/main.rs` — 0
</verification>

<success_criteria>
- The `conversations/` and `cloud/` top-level directories are completely removed
- All code from those directories is absorbed into `ai/` and `cloud_accounts/` respectively
- `main.rs` declares 18 modules (down from 20)
- `cargo build` passes with zero errors
- No stale `crate::conversations::` or `crate::cloud::` import paths exist anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-domain-consolidation-merge-scattered-modules-into-cohesive-domain-folders/02.1-01-SUMMARY.md`
</output>
