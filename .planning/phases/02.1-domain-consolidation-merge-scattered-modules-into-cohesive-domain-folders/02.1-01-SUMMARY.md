---
phase: 02.1-domain-consolidation
plan: 01
subsystem: api
tags: [rust, modules, refactor, cargo]

# Dependency graph
requires:
  - phase: 02-api-architecture-refactor
    provides: "Domain-colocated folder structure (jobs, connections, cloud_accounts, conversations)"
provides:
  - "ai/ absorbs conversations/ — db.rs, models.rs, routes.rs now siblings of client.rs in ai/"
  - "cloud_accounts/ absorbs cloud/ — reader.rs, resolver.rs now siblings in cloud_accounts/"
  - "Compile-clean codebase with all imports updated to new module paths"
affects:
  - "02.1-02 (discovery/jobs domain consolidation)"
  - "03-auth-routes-session-middleware"

# Tech tracking
tech-stack:
  added: []
  patterns: ["Rust module absorption: files moved into destination domain, super:: imports resolve to new parent"]

key-files:
  created: []
  modified:
    - server/src/ai/db.rs
    - server/src/ai/models.rs
    - server/src/ai/routes.rs
    - server/src/cloud_accounts/reader.rs
    - server/src/cloud_accounts/resolver.rs
    - server/src/cloud_accounts/mod.rs
    - server/src/jobs/db.rs
    - server/src/jobs/mod.rs
    - server/src/jobs/models.rs
    - server/src/jobs/routes.rs
    - server/src/jobs/runner.rs
    - server/src/routes/providers.rs
    - server/src/routes/scripts.rs
    - server/src/main.rs

key-decisions:
  - "All crate::conversations:: paths replaced with crate::ai:: — no behavioral changes"
  - "All crate::cloud:: paths replaced with crate::cloud_accounts:: — no behavioral changes"
  - "ProgramQuery struct moved from pipeline/mod.rs into jobs/mod.rs to serve jobs/pipeline_extract.rs via super::"
  - "Dead module directories (conversations/, cloud/) fully deleted from server/src/"

patterns-established:
  - "Module absorption: copy files into destination, update super:: references, delete source directory"

requirements-completed: [API-10]

# Metrics
duration: 8min
completed: 2026-02-26
---

# Phase 02.1 Plan 01: Absorb conversations/ into ai/ and cloud/ into cloud_accounts/ Summary

**Two smallest domain merges: conversations/db.rs+models.rs+routes.rs absorbed into ai/, cloud/reader.rs+resolver.rs absorbed into cloud_accounts/, all stale import paths fixed, cargo build passes clean**

## Performance

- **Duration:** 8 min
- **Started:** 2026-02-26T13:00:00Z
- **Completed:** 2026-02-26T13:12:51Z
- **Tasks:** 2
- **Files modified:** 13

## Accomplishments
- `conversations/` directory deleted; its 4 files (mod.rs, db.rs, models.rs, routes.rs) now live in `ai/` alongside `client.rs`
- `cloud/` directory deleted; its 3 files (mod.rs, reader.rs, resolver.rs) merged into `cloud_accounts/`
- All `crate::conversations::` and `crate::cloud::` import paths updated across the codebase
- `cargo build` compiles with zero errors after both merges

## Task Commits

Each task was committed atomically:

1. **Task 1: Absorb conversations/ into ai/ (pre-staged)** - `f5a8de8` (feat)
2. **Task 2: Absorb cloud/ into cloud_accounts/ (pre-staged)** - `f5a8de8` (feat)
3. **Fix: Update all stale import paths to compile-clean state** - `9c4e14e` (fix)

## Files Created/Modified
- `server/src/ai/db.rs` - Updated `crate::graph::rdf_graph` → `crate::discovery::rdf_graph`
- `server/src/ai/models.rs` - Updated `crate::graph::rdf_graph` → `crate::discovery::rdf_graph`
- `server/src/ai/routes.rs` - Updated graph and pipeline import paths
- `server/src/jobs/db.rs` - Updated `crate::pipeline::PipelineSummary` → `super::pipeline_types::PipelineSummary`
- `server/src/jobs/mod.rs` - Added `ProgramQuery` struct (moved from pipeline/mod.rs)
- `server/src/jobs/models.rs` - Updated dcat and pipeline import paths
- `server/src/jobs/routes.rs` - Updated rdf_format and rewrite import paths
- `server/src/jobs/runner.rs` - Updated dcat, rdf, graph, pipeline, script import paths
- `server/src/routes/providers.rs` - Updated `crate::script` → `crate::jobs::script`
- `server/src/routes/scripts.rs` - Updated pipeline and script import paths

## Decisions Made
- `ProgramQuery` struct moved into `jobs/mod.rs` rather than a dedicated file — it supports `pipeline_extract.rs` directly as a `super::` sibling, keeping the pattern consistent with how `pipeline/mod.rs` previously hosted it alongside `pipeline/extract.rs`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Build was broken by pre-staged commit with incomplete import updates**
- **Found during:** Verification step after discovering `cargo build` failing with 45 errors
- **Issue:** The previous commit `f5a8de8` moved the files but left all import paths pointing to old modules (`crate::graph::`, `crate::pipeline::`, `crate::dcat::`, `crate::rdf::`, `crate::script::`) that no longer exist in `main.rs`'s module declarations
- **Fix:** Systematically updated all 10 affected files to use new import paths under `crate::discovery::` and `crate::jobs::` namespaces; moved `ProgramQuery` struct from old `pipeline/mod.rs` into `jobs/mod.rs` to satisfy `super::ProgramQuery` reference in `jobs/pipeline_extract.rs`
- **Files modified:** ai/db.rs, ai/models.rs, ai/routes.rs, jobs/db.rs, jobs/mod.rs, jobs/models.rs, jobs/routes.rs, jobs/runner.rs, routes/providers.rs, routes/scripts.rs
- **Verification:** `cargo build` passes with zero errors, `grep -rn "crate::conversations::"` and `grep -rn "crate::cloud::"` return zero matches
- **Committed in:** `9c4e14e`

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug)
**Impact on plan:** Required to satisfy the must-have truth "cargo build compiles with zero errors after both merges". No scope creep — all fixes are import path corrections only.

## Issues Encountered
- The previous commit had simultaneously moved files AND updated `main.rs`/`routes/mod.rs` to reference new module paths (`discovery/`, `jobs::pipeline_types`, etc.) that were set up for future plans (02.1-02 and beyond), but the source files inside those modules still referenced the old paths. This created a build-broken intermediate state that this plan's execution had to resolve.

## Next Phase Readiness
- `ai/` and `cloud_accounts/` domains are clean and complete
- Plan 02.1-02 can proceed with graph→discovery and pipeline/script→jobs consolidation
- `cargo build` passes cleanly — safe foundation for next refactor step

---
*Phase: 02.1-domain-consolidation*
*Completed: 2026-02-26*
