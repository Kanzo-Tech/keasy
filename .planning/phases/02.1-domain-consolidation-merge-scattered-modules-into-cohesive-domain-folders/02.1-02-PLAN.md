---
phase: 02.1-domain-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - server/src/discovery/mod.rs
  - server/src/discovery/convert.rs
  - server/src/discovery/loader.rs
  - server/src/discovery/rdf_graph.rs
  - server/src/discovery/routes.rs
  - server/src/discovery/dcat_extract.rs
  - server/src/discovery/dcat_generator.rs
  - server/src/discovery/dcat_types.rs
  - server/src/discovery/rdf_format.rs
  - server/src/discovery/validation.rs
  - server/src/discovery/validation_routes.rs
  - server/src/discovery/validation_types.rs
  - server/src/jobs/mod.rs
  - server/src/jobs/pipeline_extract.rs
  - server/src/jobs/pipeline_types.rs
  - server/src/jobs/script.rs
  - server/src/jobs/rewrite.rs
  - server/src/jobs/runner.rs
  - server/src/routes/mod.rs
  - server/src/routes/scripts.rs
  - server/src/routes/providers.rs
  - server/src/ai/routes.rs
  - server/src/main.rs
autonomous: true
requirements:
  - API-10

must_haves:
  truths:
    - "graph/, dcat/, rdf/, validation/ directories no longer exist — all discovery code lives in discovery/"
    - "pipeline/, script/ directories no longer exist — all pipeline/script code lives in jobs/"
    - "All use crate::graph:: paths replaced with use crate::discovery::"
    - "All use crate::dcat:: paths replaced with use crate::discovery::"
    - "All use crate::rdf:: paths replaced with use crate::discovery::"
    - "All use crate::validation:: paths replaced with use crate::discovery::"
    - "All use crate::pipeline:: paths replaced with use crate::jobs::"
    - "All use crate::script:: paths replaced with use crate::jobs::"
    - "main.rs declares exactly 14 top-level modules"
    - "cargo build && cargo clippy -- -D warnings passes with zero errors and zero warnings"
  artifacts:
    - path: "server/src/discovery/"
      provides: "All graph, DCAT catalog, RDF format, and validation functionality"
    - path: "server/src/discovery/mod.rs"
      provides: "Module declarations and re-exports for all discovery submodules"
    - path: "server/src/discovery/dcat_extract.rs"
      provides: "DCAT input extraction (moved from dcat/extract.rs)"
    - path: "server/src/discovery/dcat_generator.rs"
      provides: "DCAT catalog generation (moved from dcat/generator.rs)"
    - path: "server/src/discovery/dcat_types.rs"
      provides: "DCAT data types (moved from dcat/types.rs)"
    - path: "server/src/discovery/rdf_format.rs"
      provides: "RDF export format enum (moved from rdf/format.rs)"
    - path: "server/src/discovery/validation.rs"
      provides: "ValidatableGraph (moved from validation/mod.rs)"
    - path: "server/src/discovery/validation_routes.rs"
      provides: "Validation route handler (moved from validation/routes.rs)"
    - path: "server/src/discovery/validation_types.rs"
      provides: "Validation types — ShapeFormat, ValidationRequest, ValidationResult (moved from validation/types.rs)"
    - path: "server/src/jobs/pipeline_extract.rs"
      provides: "Pipeline summary extraction (moved from pipeline/extract.rs)"
    - path: "server/src/jobs/pipeline_types.rs"
      provides: "Pipeline types — PipelineSummary, PipelineOutput, Field, etc. (moved from pipeline/types.rs)"
    - path: "server/src/jobs/script.rs"
      provides: "Script compile/execute/init_context (moved from script/mod.rs)"
    - path: "server/src/jobs/rewrite.rs"
      provides: "Script rewrite functions (moved from script/rewrite.rs)"
  key_links:
    - from: "server/src/discovery/mod.rs"
      to: "all discovery submodules"
      via: "pub mod declarations"
      pattern: "pub mod (convert|loader|rdf_graph|routes|dcat_|rdf_format|validation)"
    - from: "server/src/jobs/mod.rs"
      to: "pipeline_types, pipeline_extract, script, rewrite"
      via: "pub mod + pub use re-exports"
      pattern: "pub use pipeline_types::"
    - from: "server/src/routes/mod.rs"
      to: "crate::discovery::routes, crate::ai::routes, crate::jobs::routes"
      via: "handler references in router"
      pattern: "crate::discovery::routes::"
    - from: "server/src/main.rs"
      to: "14 top-level modules"
      via: "mod declarations"
      pattern: "mod discovery;"
---

<objective>
Rename `graph/` to `discovery/` absorbing `dcat/`, `rdf/`, and `validation/`; absorb `pipeline/` and `script/` into `jobs/`. Final cleanup of `main.rs` and full cargo build + clippy gate.

Purpose: This completes the domain consolidation. After this plan, every source file lives in the domain it belongs to. The server has 14 top-level modules (down from 20), each with a clear single-domain responsibility. The codebase is clean for Phase 3 auth layering.

Output: `discovery/` domain folder (12 files), expanded `jobs/` folder (10 files), clean `main.rs` with 14 module declarations. `cargo build && cargo clippy` pass.
</objective>

<execution_context>
@/Users/angel.ip/.claude/get-shit-done/workflows/execute-plan.md
@/Users/angel.ip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1-domain-consolidation-merge-scattered-modules-into-cohesive-domain-folders/02.1-RESEARCH.md
@.planning/phases/02.1-domain-consolidation-merge-scattered-modules-into-cohesive-domain-folders/02.1-01-SUMMARY.md

Source files being moved (graph -> discovery):
@server/src/graph/mod.rs
@server/src/graph/convert.rs
@server/src/graph/loader.rs
@server/src/graph/rdf_graph.rs
@server/src/graph/routes.rs

Source files being absorbed into discovery:
@server/src/dcat/mod.rs
@server/src/dcat/extract.rs
@server/src/dcat/generator.rs
@server/src/dcat/types.rs
@server/src/rdf/mod.rs
@server/src/rdf/format.rs
@server/src/validation/mod.rs
@server/src/validation/routes.rs
@server/src/validation/types.rs

Source files being absorbed into jobs:
@server/src/pipeline/mod.rs
@server/src/pipeline/extract.rs
@server/src/pipeline/types.rs
@server/src/script/mod.rs
@server/src/script/rewrite.rs

Key callers:
@server/src/main.rs
@server/src/routes/mod.rs
@server/src/routes/scripts.rs
@server/src/routes/providers.rs
@server/src/jobs/runner.rs
@server/src/jobs/mod.rs
@server/src/ai/routes.rs (updated in Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create discovery/ domain — rename graph/, absorb dcat/, rdf/, validation/</name>
  <files>
    server/src/discovery/ (new directory, all files)
    server/src/graph/ (deleted)
    server/src/dcat/ (deleted)
    server/src/rdf/ (deleted)
    server/src/validation/ (deleted)
    server/src/routes/mod.rs
    server/src/jobs/runner.rs
    server/src/ai/routes.rs
    server/src/main.rs
  </files>
  <action>
Create the `discovery/` domain folder by renaming `graph/` and absorbing `dcat/`, `rdf/`, and `validation/`:

**Step 1 — Create `discovery/` directory and move `graph/` files:**
- `graph/convert.rs` -> `discovery/convert.rs`
- `graph/loader.rs` -> `discovery/loader.rs`
- `graph/rdf_graph.rs` -> `discovery/rdf_graph.rs`
- `graph/routes.rs` -> `discovery/routes.rs`
- `graph/mod.rs` -> base for `discovery/mod.rs`

**Step 2 — Move `dcat/` files into `discovery/` with flat naming:**
- `dcat/extract.rs` -> `discovery/dcat_extract.rs`
- `dcat/generator.rs` -> `discovery/dcat_generator.rs`
- `dcat/types.rs` -> `discovery/dcat_types.rs`

**Step 3 — Move `rdf/` files into `discovery/`:**
- `rdf/format.rs` -> `discovery/rdf_format.rs`

**Step 4 — Move `validation/` files into `discovery/`:**
- `validation/mod.rs` -> `discovery/validation.rs` (contains `ValidatableGraph` struct and methods)
- `validation/routes.rs` -> `discovery/validation_routes.rs`
- `validation/types.rs` -> `discovery/validation_types.rs`

**Step 5 — Create `discovery/mod.rs`:**
Start from `graph/mod.rs` content, then add declarations for all absorbed modules:
```rust
pub mod convert;
pub mod loader;
pub mod rdf_graph;
pub mod routes;
pub mod dcat_extract;
pub mod dcat_generator;
pub mod dcat_types;
pub mod rdf_format;
pub mod validation;
pub mod validation_routes;
pub mod validation_types;
```
Also absorb any re-exports or inline logic from `dcat/mod.rs` and `rdf/mod.rs`. The `dcat/mod.rs` likely has `pub use` re-exports — replicate them in `discovery/mod.rs`. The `rdf/mod.rs` likely just declares `pub mod format;` — the content is now `rdf_format.rs`.

**Step 6 — Update internal imports in moved files:**
- `discovery/routes.rs` (was `graph/routes.rs`):
  - `use crate::cloud::reader` -> `use crate::cloud_accounts::reader` (already changed in Plan 01)
  - `use crate::rdf::format::RdfExportFormat` -> `use super::rdf_format::RdfExportFormat` (now sibling)
  - Any `use crate::graph::` -> `use super::` or `use crate::discovery::`
- `discovery/dcat_generator.rs` (was `dcat/generator.rs`):
  - `use crate::rdf::format::RdfExportFormat` -> `use super::rdf_format::RdfExportFormat`
  - `use crate::graph::rdf_graph::RdfGraph` -> `use super::rdf_graph::RdfGraph`
  - Any other `crate::dcat::` or `crate::graph::` -> `use super::`
- `discovery/dcat_extract.rs` (was `dcat/extract.rs`):
  - `use crate::dcat::types::*` or similar -> `use super::dcat_types::*`
- `discovery/validation.rs` (was `validation/mod.rs`):
  - `use crate::cloud::reader` -> `use crate::cloud_accounts::reader`
  - `use crate::validation::types::*` -> `use super::validation_types::*`
- `discovery/validation_routes.rs` (was `validation/routes.rs`):
  - `use crate::cloud::reader` -> `use crate::cloud_accounts::reader`
  - `use crate::validation::` -> `use super::validation` or `use super::validation_types::`

**Step 7 — Update all external callers** (search for `crate::graph::`, `crate::dcat::`, `crate::rdf::`, `crate::validation::`):
- `main.rs`:
  - Remove `mod graph;`, `mod dcat;`, `mod rdf;`, `mod validation;`
  - Add `mod discovery;`
  - `use graph::rdf_graph::RdfGraph` -> `use discovery::rdf_graph::RdfGraph`
  - `use graph::loader::parse_rdf_to_triples` -> `use discovery::loader::parse_rdf_to_triples` (if present)
- `routes/mod.rs`:
  - All `crate::graph::routes::` -> `crate::discovery::routes::`
  - All `crate::validation::routes::` -> `crate::discovery::validation_routes::`
- `jobs/runner.rs`:
  - `use crate::graph::rdf_graph::RdfGraph` -> `use crate::discovery::rdf_graph::RdfGraph`
  - `use crate::graph::loader::*` -> `use crate::discovery::loader::*`
  - `use crate::dcat::extract::*` -> `use crate::discovery::dcat_extract::*`
  - `use crate::dcat::generator::*` -> `use crate::discovery::dcat_generator::*`
  - `use crate::dcat::types::*` -> `use crate::discovery::dcat_types::*`
  - `use crate::rdf::format::RdfExportFormat` -> `use crate::discovery::rdf_format::RdfExportFormat`
- `ai/routes.rs`:
  - `use crate::graph::rdf_graph::RdfGraph` -> `use crate::discovery::rdf_graph::RdfGraph` (if present)

**Step 8 — Delete** `graph/`, `dcat/`, `rdf/`, `validation/` directories.

**Step 9 — Run `cargo build`** to verify. Fix any missed import paths surfaced by the compiler.

Do NOT change any behavior, error handling, API paths, or function signatures. This is purely a file move + import update. Specifically, do NOT modernize `validation_routes.rs` error handling — it uses old `error_body` pattern intentionally per CONTEXT.md.
  </action>
  <verify>
    <automated>cd /Users/angel.ip/dev/kanzo/keasy/keasy/server && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `graph/`, `dcat/`, `rdf/`, `validation/` directories all deleted
    - `discovery/` contains 12 files: mod.rs, convert.rs, loader.rs, rdf_graph.rs, routes.rs, dcat_extract.rs, dcat_generator.rs, dcat_types.rs, rdf_format.rs, validation.rs, validation_routes.rs, validation_types.rs
    - No `crate::graph::`, `crate::dcat::`, `crate::rdf::`, or `crate::validation::` references remain
    - `cargo build` compiles with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Absorb pipeline/ and script/ into jobs/, final main.rs cleanup, cargo clippy gate</name>
  <files>
    server/src/jobs/mod.rs
    server/src/jobs/pipeline_extract.rs
    server/src/jobs/pipeline_types.rs
    server/src/jobs/script.rs
    server/src/jobs/rewrite.rs
    server/src/jobs/runner.rs
    server/src/pipeline/ (deleted)
    server/src/script/ (deleted)
    server/src/routes/scripts.rs
    server/src/routes/providers.rs
    server/src/ai/routes.rs
    server/src/main.rs
  </files>
  <action>
Absorb `pipeline/` and `script/` into `jobs/`, then perform final cleanup:

**Step 1 — Move `pipeline/` files into `jobs/` with flat naming:**
- `pipeline/extract.rs` -> `jobs/pipeline_extract.rs`
- `pipeline/types.rs` -> `jobs/pipeline_types.rs`

**Step 2 — Move `script/` files into `jobs/`:**
- `script/mod.rs` -> `jobs/script.rs` (the compile/execute/init_context logic)
- `script/rewrite.rs` -> `jobs/rewrite.rs`

**Step 3 — Update `jobs/mod.rs`:**
Add new module declarations:
```rust
pub mod pipeline_extract;
pub mod pipeline_types;
pub mod script;
pub mod rewrite;
```

Add re-exports to preserve the public surface that external callers used via `crate::pipeline::`:
```rust
// Re-exports (callers used crate::pipeline::PipelineSummary etc.)
pub use pipeline_types::{PipelineSummary, PipelineOutput, Field, ValidationResult as PipelineValidationResult};
pub use pipeline_extract::extract_summary;
```
Note: Check `pipeline/mod.rs` for the exact list of `pub use` re-exports and replicate them. Also check `pipeline/mod.rs` for any inline logic (like `ProgramQuery` struct) — if present, include it in `jobs/mod.rs` or in a dedicated `jobs/pipeline.rs`.

**Step 4 — Update internal imports in moved files:**
- `jobs/script.rs` (was `script/mod.rs`):
  - `use crate::cloud::is_data_path` should already be `use crate::cloud_accounts::is_data_path` (from Plan 01). Verify.
  - Any `use super::rewrite` -> `use super::rewrite` (still correct — both now in `jobs/`)
  - Any `use crate::script::rewrite` -> `use super::rewrite`
- `jobs/rewrite.rs` (was `script/rewrite.rs`):
  - `use crate::cloud::is_data_path` should already be `use crate::cloud_accounts::is_data_path`. Verify.
  - Any `use super::` references that pointed to `script/mod.rs` — now `super` is `jobs`. Verify all resolve.
- `jobs/pipeline_extract.rs` (was `pipeline/extract.rs`):
  - `use crate::pipeline::types::*` or `use super::types::*` -> `use super::pipeline_types::*`
  - Any `use crate::pipeline::ProgramQuery` -> `use super::ProgramQuery` (if ProgramQuery moved to jobs/mod.rs)
- `jobs/pipeline_types.rs` (was `pipeline/types.rs`):
  - Likely has no internal imports to change. Verify.

**Step 5 — Update `jobs/runner.rs` imports:**
- `use crate::pipeline::PipelineOutput` -> `use super::pipeline_types::PipelineOutput` (or `use super::PipelineOutput` if re-exported)
- `use crate::script` -> `use super::script`
- Any remaining `crate::pipeline::` or `crate::script::` -> `super::` or `crate::jobs::`

**Step 6 — Update all external callers** (search for `crate::pipeline::` and `crate::script::`):
- `routes/scripts.rs`:
  - `use crate::pipeline::{PipelineSummary, ValidationResult, extract_summary}` -> `use crate::jobs::{PipelineSummary, extract_summary}` (using re-exports from jobs/mod.rs). Note: `ValidationResult` may need disambiguation with `PipelineValidationResult` if there's a name conflict.
  - `use crate::script` -> `use crate::jobs::script`
  - `use crate::script::rewrite` -> `use crate::jobs::rewrite`
- `routes/providers.rs`:
  - `use crate::script::init_context` -> `use crate::jobs::script::init_context`
- `ai/routes.rs`:
  - `use crate::pipeline::PipelineSummary` -> `use crate::jobs::PipelineSummary` (if present, using re-export)

**Step 7 — Update `main.rs`:**
- Remove `mod pipeline;`
- Remove `mod script;`
- Verify final module list is exactly 14:
  ```rust
  mod ai;
  mod cloud_accounts;
  mod config;
  mod connections;
  mod crypto;
  mod db;
  mod discovery;
  mod error;
  mod jobs;
  mod middleware;
  mod routes;
  mod settings;
  mod tenant;
  ```
  Wait — count: ai, cloud_accounts, config, connections, crypto, db, discovery, error, jobs, middleware, routes, settings, tenant = 13. Verify by checking if there are any other `mod` declarations that should remain. The research says 14 but double check — there may be a `mod` for the `main` function's own module scope. Regardless, the number should match exactly what's needed — no more, no less.

**Step 8 — Delete** `pipeline/` and `script/` directories.

**Step 9 — Run `cargo build && cargo clippy -- -D warnings`** to verify full clean compilation.

Do NOT change any behavior, error handling, API paths, or function signatures.
  </action>
  <verify>
    <automated>cd /Users/angel.ip/dev/kanzo/keasy/keasy/server && cargo build && cargo clippy -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `pipeline/` and `script/` directories deleted
    - `jobs/` contains: mod.rs, db.rs, errors.rs, models.rs, routes.rs, runner.rs, pipeline_extract.rs, pipeline_types.rs, script.rs, rewrite.rs
    - No `crate::pipeline::` or `crate::script::` references remain anywhere
    - `main.rs` declares only the consolidated top-level modules (ai, cloud_accounts, config, connections, crypto, db, discovery, error, jobs, middleware, routes, settings, tenant)
    - `cargo build` compiles with zero errors
    - `cargo clippy -- -D warnings` passes with zero warnings
  </done>
</task>

</tasks>

<verification>
1. `cargo build && cargo clippy -- -D warnings` — zero errors, zero warnings
2. `grep -rn "crate::graph::\|crate::dcat::\|crate::rdf::\|crate::validation::\|crate::pipeline::\|crate::script::" server/src/` — zero matches
3. `ls server/src/graph/ server/src/dcat/ server/src/rdf/ server/src/validation/ server/src/pipeline/ server/src/script/` — all directories do not exist
4. `ls server/src/discovery/` — 12 .rs files present
5. `ls server/src/jobs/` — 10 .rs files present
6. `grep "^mod " server/src/main.rs | wc -l` — exactly 13 (or 14, verify)
7. `grep -rn "mod graph\|mod dcat\|mod rdf\|mod validation\|mod pipeline\|mod script" server/src/main.rs` — zero matches
</verification>

<success_criteria>
- All 6 dissolved modules (graph, dcat, rdf, validation, pipeline, script) are completely removed
- `discovery/` contains all graph/catalog/validation code (12 files)
- `jobs/` contains all job/pipeline/script code (10 files)
- `main.rs` has only the final consolidated module set
- `cargo build && cargo clippy -- -D warnings` passes clean
- No stale import paths exist anywhere in the codebase
- API behavior is completely unchanged — all endpoints work identically
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-domain-consolidation-merge-scattered-modules-into-cohesive-domain-folders/02.1-02-SUMMARY.md`
</output>
