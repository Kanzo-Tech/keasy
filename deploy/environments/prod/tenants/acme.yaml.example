# Template — copy to `acme.yaml` (drop `.example`) to provision a tenant.
# The filename stem (`acme`) is the slug → https://acme.<base_domain>.
name: Acme Corp
owner_keycloak_sub: 00000000-0000-0000-0000-000000000000  # the owner's Keycloak user id

# Optional overrides (omit to inherit versions.env):
# slug: acme-eu
# server_image: ghcr.io/kanzo-tech/keasy-server:0.4.0-rc.1   # canary this tenant
# web_image: ghcr.io/kanzo-tech/keasy-web:0.4.0-rc.1
