# State holds every tenant secret — manager-only, never committed.
*.tfstate
*.tfstate.*
.terraform/
.terraform.tfstate.lock.info
# Operator-local tenant registry + creds (PII/secrets stay off git).
terraform.tfvars
# Keep .terraform.lock.hcl tracked.

# but DO track the git version-of-record:
!images.auto.tfvars
