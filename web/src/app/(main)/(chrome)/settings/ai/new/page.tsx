"use client";

export default function NewAiProviderPage() {
  return (
    <div className="flex items-center justify-center p-12 text-muted-foreground">
      AI provider configuration — pending schema-driven rewrite
    </div>
  );
}
