"use client";

export default function CatalogStoragePage() {
  return (
    <div className="flex items-center justify-center p-12 text-muted-foreground">
      Catalog storage — removed (DCAT materializer deleted)
    </div>
  );
}
